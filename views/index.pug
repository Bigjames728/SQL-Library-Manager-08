extends layout

block content
  h1 Books
  p 
    table 
      thead 
        tr 
          th id 
          th Title 
          th Author 
          th Genre 
          th Year 
      tbody
        each book in books 
         tr 
          td #{book.id}
          td #{book.title}
          td #{book.author}
          td #{book.genre}
          td #{book.year}